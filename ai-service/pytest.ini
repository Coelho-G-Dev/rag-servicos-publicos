[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    integration: Testes de integração reais com banco e Docker (não rodam por padrão no CI)
addopts = -m "not integration" --strict-markers
